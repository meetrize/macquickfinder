---
description: Git 提交信息必须使用简体中文（含 Cursor 自动生成与 Agent 代写）
alwaysApply: true
---

# Git 提交信息语言

生成或撰写 **任何 Git commit message**（包括 Source Control 面板的「生成提交信息」、Agent 代写提交、用户要求写 commit message）时，**必须使用简体中文**。

## 要求

- 标题与正文均使用简体中文，不要输出英文提交说明（类型前缀如 `feat:`、`fix:`、`perf:` 可保留，其后描述须为中文）。
- 若仓库历史多为英文提交，仍以本规则为准，优先输出中文。
- 语气简洁，说明「做了什么」与「为什么」，避免空泛表述。
- 一次提交聚焦一个主题；正文可选，仅在需要补充上下文时写。

## 示例（本项目）

```
feat: 侧边栏 Devices 动态显示已挂载移动硬盘

fix: 修复路径切换后目录列表不刷新的问题

perf: 消除文件属性重复读取并预计算列表格式化字段

docs: 添加 Phase 1 性能优化方案文档

chore: 更新构建脚本与忽略规则
```

## 优先级

本规则优先级高于默认英文习惯；与用户明确要求其他语言时，以用户当次指令为准。
