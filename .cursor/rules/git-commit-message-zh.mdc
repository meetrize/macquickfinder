---
description: Git 提交信息与 Source Control「生成提交信息」默认使用简体中文
alwaysApply: true
---

# Git 提交信息语言（简体中文）

生成或撰写 **任何 Git commit message** 时，**必须使用简体中文**，包括但不限于：

- **Source Control → 生成提交信息**（✨  sparkle 按钮）
- Agent / Chat 代写提交
- 用户说「写 commit」「生成提交说明」

> 说明：SCM 自动生成提交信息主要读取仓库根目录 `.cursorrules`；本规则供 Agent 与对话场景，二者内容保持一致。

## 要求

- 标题与正文均使用简体中文；类型前缀如 `feat:`、`fix:` 可保留，**其后描述须为中文**
- 语气简洁，说明「做了什么」与「为什么」
- 一次提交聚焦一个主题；正文可选

## 示例

```
feat: 侧边栏 Devices 动态显示已挂载移动硬盘

fix: 修复路径切换后目录列表不刷新的问题

perf: 消除文件属性重复读取并预计算列表格式化字段

docs: 添加 i18n Phase 3 实施说明

chore: 更新构建脚本与忽略规则
```

## 优先级

本规则优先级高于默认英文习惯；**仅当用户当次明确要求其他语言**时例外。
