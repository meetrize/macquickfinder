---
description: 新增 UI 文案时必须同步 String Catalog 并编译 .lproj，禁止界面只显示语言键
alwaysApply: true
---

# UI 多语言文案（i18n）规则

新增或修改**用户可见文案**时，必须保证运行时显示翻译后的文本，**禁止**界面上出现 `settings.foo.bar` / `snippets.ask.form_title` 这类语言键。

## 根因（必读）

本项目是 **SPM**：`swift build` **不会**把 `Localizable.xcstrings` 编进可加载文案。  
运行时 `ModuleLocalization` 读的是：

`Sources/<Module>/Resources/<lang>.lproj/Localizable.strings`

因此仅改 `.xcstrings` 却**未**运行同步编译 → **必然显示键名**。这是历史反复出现的事故，文档写「写 xcstrings」不够，**必须同步 `.lproj`**。

## 单一真相来源

| 模块 | 权威文件（编辑） | 运行时产物（必须同步） |
|------|------------------|------------------------|
| Explorer | `Sources/Explorer/Resources/Localizable.xcstrings` | `…/en.lproj` + `…/zh-Hans.lproj` 的 `Localizable.strings` |
| FileList | `Sources/FileList/Resources/Localizable.xcstrings` | 同上 |

## 新增 UI 文案的标准流程（缺一步都不算完成）

1. **在 `Localizable.xcstrings` 添加键**，补齐 `en` 与 `zh-Hans`（`state: translated`）。
2. **在 `L10n.swift` 增加类型安全访问**（`L10n.Section.key`）。
3. **UI 只引用 `L10n`**，不要硬编码键名或 `ModuleLocalization.localized("…")` 散落在视图层。
4. **立刻执行**（写入 xcstrings 后、声称完成前）：
   ```bash
   bash Scripts/compile_localizations.sh
   ```
   该脚本会用 `xcstringstool` 或 Python 兜底生成 `.lproj`，并校验 catalog ⊆ `en.lproj`；失败则不得交付。
5. **自测**：`grep 'your.key' Sources/.../zh-Hans.lproj/Localizable.strings` 能搜到；切换中/英界面确认不是键名。
6. **补充测试**：`L10nTests` 增加 `XCTAssertNotEqual(L10n.…, "键名")`。

## 禁止

- 只改 `L10n.swift` / 只改 `.xcstrings`，不跑 `compile_localizations.sh`
- 只改 `en.lproj` / `zh-Hans.lproj` 的 `.strings`，不改 `.xcstrings`
- 依赖「用户之后会 build」而不在当前会话同步 `.lproj`
- 无 `xcstringstool` 时以为可以跳过编译（现已有 Python 兜底，不可跳过）

## 自检清单（Agent 完成前必勾）

- [ ] 新键已在对应 `Localizable.xcstrings`
- [ ] `en` + `zh-Hans` 均有译文
- [ ] `L10n.swift` 已暴露
- [ ] **已运行 `bash Scripts/compile_localizations.sh` 且成功**
- [ ] 新键已出现在 `zh-Hans.lproj/Localizable.strings`
- [ ] `L10nTests` 覆盖关键新键

## 示例

```
// ✅ 正确（且已 compile_localizations）
Text(L10n.Settings.Shortcuts.globalToggle)

// ❌ 错误：键未进 xcstrings / 未编译进 .lproj → 显示 settings.shortcut.global_toggle
Text(ModuleLocalization.localized("settings.shortcut.global_toggle", bundle: .module))
```

细则见 `docs/i18n-design.md` §「SPM 与 .lproj 同步」。
