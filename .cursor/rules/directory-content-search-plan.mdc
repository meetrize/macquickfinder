---
description: 当前目录全文搜索（方案 A）开发约束与计划索引；实现 CS-01~CS-25 时必须遵循，完成后自动进入下一步
alwaysApply: false
---

# 当前目录全文搜索 — Cursor 开发计划规则

当用户或任务涉及**全文搜索**、**内容搜索**、**Find in Folder**、**CS-0x**、或 `directory-content-search` 文档时，必须按本规则执行。

## 权威文档

| 文档 | 用途 |
|------|------|
| [docs/directory-content-search-design.md](../../docs/directory-content-search-design.md) | 交互、架构、过滤、预览联动、i18n |
| [docs/directory-content-search-plan.md](../../docs/directory-content-search-plan.md) | Issue 拆分、依赖、验收、测试清单 |

**设计冲突时**：以 design 为准；plan 负责排期与 checkbox。

## 自动推进协议（必须遵守）

1. **读取 plan** 找到第一个 `- [ ]` 未勾选的 CS Issue（按 Issue 索引顺序，不得跳步）。
2. **实现该 Issue** 全部任务与验收标准。
3. **验证**：`swift build` + `swift test --filter DirectoryContentSearch`（及本 Issue 相关 filter）。
4. **更新 plan**：将该 Issue 任务 checkbox 改为 `- [x]`。
5. **Git**：用户未要求时不提交；若用户要求提交，commit message 用简体中文。
6. **立即进入下一个 CS Issue**，无需等待用户确认，直到 P5（CS-22）完成或遇到阻塞。
7. **P6（CS-23~25）为可选**：P5 完成后暂停并询问用户是否继续进阶能力。

## Issue 执行顺序（严格）

```
P1  CS-01 模型 → CS-02 Glob → CS-03 引擎 → CS-04 Session → CS-05 结果视图 → CS-06 摘要栏
P2  CS-07 ContentView 切换 → CS-08 顶栏模式 → CS-09 过滤条 → CS-10 引擎 filter → CS-11 展开策略
P3  CS-12 预览跳转 → CS-13 键盘导航 → CS-14 Quick Search 互斥 → CS-15 ⌘G 协调
P4  CS-16 i18n → CS-17 菜单/Shortcut → CS-18 Command Palette
P5  CS-19 并行扫描 → CS-20 取消/Esc → CS-21 二进制检测 → CS-22 集成测试
P6  CS-23 正则 → CS-24 PDF/Office → CS-25 rg 后端（可选）
```

## 范围约束

### 必须做

- 方案 A：内容搜索有结果时主区域显示 `DirectoryContentSearchResultsView`，非右栏 Tab
- `searchText`（文件名过滤）与 `contentQuery`（内容搜索）**字段分离**
- 内容搜索激活时禁用 Quick Search 字母拦截
- 复用 `PreviewTextSearchHighlighter`、`FileListTextHighlight`
- 新 UI 文案：`Localizable.xcstrings` + `L10n.swift` + `L10nTests`（CS-16 起，之前可用英文占位）

### 禁止做（首版 / P5 前）

- 合并 `searchText` 与内容 query 语义
- Spotlight 全局索引
- Replace in Files
- 在单个 Issue 混合无关重构
- 仅改 `*.lproj/Localizable.strings` 不改 `.xcstrings`

## 模块路径

```
Sources/Explorer/DirectoryContentSearch/
Tests/ExplorerTests/DirectoryContentSearch*.swift
```

## 每步最低验证

| Phase | 命令 |
|-------|------|
| P1 | `swift test --filter DirectoryContentSearch` |
| P2+ | 上述 + 手动：顶栏切内容模式可见结果 |
| P3+ | 上述 + Enter 预览跳转 |
| P4 | `swift test --filter L10nTests` 覆盖新键 |

## Git 提交

- 类型前缀可保留，**描述用简体中文**（`git-commit-message-zh.mdc`）
- 建议每个 Phase 或 2–3 个 CS 一个 commit
