# MeoFind / macquickfinder — Cursor 仓库规则

## Git 提交信息（最高优先级）

在以下**所有**场景生成 commit message 时，**必须使用简体中文**：

- Source Control 面板的「生成提交信息」（✨ 按钮）
- Agent / Chat 代写提交说明
- 用户要求撰写 commit message

### 格式

- 标题一行，类型前缀可保留：`feat:`、`fix:`、`perf:`、`docs:`、`chore:`、`refactor:`、`test:` 等，**冒号后描述必须用中文**
- 正文可选，使用中文补充「为什么」
- 语气简洁，一次提交一个主题

### 示例

```
feat: 侧边栏 Devices 动态显示已挂载移动硬盘

fix: 修复路径切换后目录列表不刷新的问题

chore: 编译 String Catalog 修复界面显示键名问题
```

### 禁止

- 不要输出英文提交标题或英文正文（除非用户当次明确要求其他语言）

## UI 多语言文案（i18n）

新增用户可见 UI 文案时，**必须**完成以下全部步骤，缺一不可：

1. 写入对应模块的 `Localizable.xcstrings`（`en` + `zh-Hans`）
2. 在 `L10n.swift` 暴露访问
3. **立即执行** `bash Scripts/compile_localizations.sh`，把 catalog 同步到 `*.lproj/Localizable.strings`

SPM **不会**自动编译 String Catalog。只改 `.xcstrings` / `L10n.swift` 而不同步 `.lproj`，界面会**原样显示语言键**（历史重复事故）。禁止只改 `*.lproj` 而不改 `.xcstrings`。细则见 `.cursor/rules/i18n-ui-strings.mdc` 与 `docs/i18n-design.md`。
